<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/fondoOscuro"
    android:padding="20dp">

    <TextView
        android:id="@+id/titulo"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="50dp"
        android:text="AI ASSISTANT NÚCLEO"
        android:textColor="@color/neonRosa"
        android:textSize="24sp"
        android:textStyle="bold" />

    <TextView
        android:id="@+id/estado_txt"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/titulo"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="10dp"
        android:text="Estado: Esperando Activación"
        android:textColor="@color/neonCyan"
        android:textSize="14sp" />

    <Button
        android:id="@+id/btn_activar"
        android:layout_width="match_parent"
        android:layout_height="60dp"
        android:layout_centerInParent="true"
        android:backgroundTint="@color/neonCyan"
        android:text="ACTIVAR BURBUJA FLOTANTE"
        android:textColor="@color/fondoOscuro"
        android:textSize="16sp"
        android:textStyle="bold" />

</RelativeLayout>

